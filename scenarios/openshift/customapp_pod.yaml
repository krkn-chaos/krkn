# yaml-language-server: $schema=../plugin.schema.json
- id: kill-pods
  config:
    namespace_pattern: ^acme-air$
    name_pattern: .*
- id: wait-for-pods
  config:
    namespace_pattern: ^acme-air$
    name_pattern: .*
    count: 8