node_scenarios:
- name: Stop kubelet
  actions:
    - stop_kubelet
  label_selector: node-role.kubernetes.io/worker
  instance_kill_count: 1
  timeout: 20
  cloud_type: aws
- name: Fork bomb the node
  actions:
    - node_crash
  label_selector: node-role.kubernetes.io/worker
  instance_kill_count: 1
  timeout: 20
  cloud_type: aws